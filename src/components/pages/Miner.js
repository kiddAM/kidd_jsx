import React from 'react';
import { Dictionary } from '../dataminer/Dictionary';

export const Miner = () => {
    return(
        <div className="page miner">
            <Dictionary />
        </div>
    );
}