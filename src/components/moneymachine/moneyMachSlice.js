import { createSlice } from '@reduxjs/toolkit'

export const moneyMachineSlice = createSlice({
  name: 'moneyMachine',
  initialState: {
    // init state values
  },
  reducers: {
    // reducers here
  }
})

// remember to export everything